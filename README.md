# AI-Pulse
AI-Pulse Onboarder is a document-powered AI chat assistant built as a React web application. Users upload company documents (PDFs, TXT), and the system extracts text, splits it into overlapping chunks, stores it in MongoDB, and exposes two modes of AI conversation: per-document chat and cross-document "Knowledge Base" chat.
